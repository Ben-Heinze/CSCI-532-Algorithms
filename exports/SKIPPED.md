# Files skipped during export

The following `.org` files from the source repository were not converted to
cleaned wiki pages. The reason is noted next to each.

- `mothicaConcert.org` — not class content (concert notes).
- `np-completeness.org` — empty scaffold (title header only, no material).
- `PTAS.org` — empty scaffold (title header only, no material).
- `hm3.org` — homework problem set / solutions rather than lecture notes. If
  you want the ILP-formulation walkthroughs from Problems 1 & 2 folded into
  the ILP page, they can be added later.
- `test3Notes.org` — a test-review pass whose material is already covered by
  the ILP and approximation pages; any non-duplicate content has been folded
  into `integer-linear-programming/` and `approximation-algorithms/`.

Editor backup files (`#*#`, `*~`) and non-`.org` artifacts (`.pdf`, `.tex`,
`.png`) were also ignored.
